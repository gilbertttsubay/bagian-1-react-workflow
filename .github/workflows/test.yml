name: Test Workflow
on:
  push:
    branches:
      - master
  workflow_dispatch:
    branches:
      - master

jobs:
  first-workflow:

    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: npm clean install
        run: |
          npm install